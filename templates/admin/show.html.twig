{% extends 'base.html.twig' %}

{% block title %}admin{% endblock %}

{% block content %}
    <h1>admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ admin.roles ? admin.roles|json_encode : '' }}</td>
            </tr>
            
            <tr>
                <th>Username</th>
                <td>{{ admin.username }}</td>
            </tr>
        </tbody>
    </table>

  <div class="btn-group" role="group" aria-label="Basic example">
                 <a  class="btn btn-outline-secondary" href="{{ path('app_admin_index') }}">back to list</a>
                    <a class="btn btn-outline-primary"  href="{{ path('app_admin_edit', {'id': admin.id}) }}">Edit</a>

                   <a class="btn btn-outline-danger"> {{ include('admin/_delete_form.html.twig') }} </a>
               
                 </div>
{% endblock %}
