{% extends 'base.html.twig' %}

{% block title %}Device{% endblock %}

{% block content %}
    <h1>Device</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ device.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ device.name }}</td>
            </tr>
            <tr>
                <th>IdDevice</th>
                <td>{{ device.idDevice }}</td>
            </tr>
        </tbody>
    </table>

   

   <div class="btn-group" role="group" aria-label="Basic example">
                     <a  class="btn btn-outline-secondary" href="{{ path('app_device_index') }}">back to list</a>

                    <a class="btn btn-outline-primary"  href="{{ path('app_device_edit', {'id': device.id}) }}">Edit</a>
                  <a class="btn btn-outline-danger" > {{ include('device/_delete_form.html.twig') }} </a>
               
                 </div>



    
{% endblock %}
